import React from 'react';

export default class LoadingPage extends React.Component {
    render() {
        return (
            <div> You are seeing this because the reviews have yet to load in. </div>
        )
    }
}