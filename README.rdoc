== README

#Installation & Usage  

To install and run this program, 

clone the git respository

type cd music-tracker

type bundle install

type rake db:migrate

type rake db:seed

type rails s

type cd client

type npm install

type npm start

and Music Rater will open in your browser!




